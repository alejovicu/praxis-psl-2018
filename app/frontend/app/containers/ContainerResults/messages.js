/*
 * ContainerResults Messages
 *
 * This contains all the text for the ContainerResults component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.ContainerResults.header',
    defaultMessage: 'This is ContainerResults container !',
  },
});
